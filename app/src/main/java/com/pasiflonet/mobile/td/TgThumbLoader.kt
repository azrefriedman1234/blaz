package com.pasiflonet.mobile.td

import android.content.Context

/**
 * Minimal stub to keep CI buildable.
 * Replace with real thumbnail extraction later.
 */
object TgThumbLoader {
    fun loadThumbBytes(context: Context, sourcePath: String): ByteArray? {
        return null
    }
}
