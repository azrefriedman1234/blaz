package com.pasiflonet.mobile.td

import android.content.Context

/**
 * Temporary stub: keeps CI buildable.
 * Replace with real thumbnail extraction later.
 */
object TgThumbLoader {
    fun loadThumbBytes(context: Context, sourcePath: String): ByteArray? = null
}
